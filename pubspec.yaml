name: be_human
description: BeHuman - Board personal entre dos personas
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0
  image_picker: ^1.0.0
  video_player: ^2.0.0
  cached_network_image: ^3.3.0
  uuid: ^4.0.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.2
  home_widget: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true